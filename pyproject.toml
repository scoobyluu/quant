[project]
name = "quant"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "George Anagnostou", email = "anagnostougeorgejohn@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "lxml>=6.1.2",
    "polars>=1.43.2",
    "yfinance>=1.6.0",
]

[project.scripts]
quant = "quant:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
